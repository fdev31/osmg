#!/bin/sh
# avoid generated files & images in the count
git ls-files | grep -Ev '[.](svg|jpg)$' |
    grep -v front/jscomp |
    grep -v front/js/locales.js |
    while read f; do git blame -w -M -C -C --line-porcelain "$f" | grep -I '^author '; done | sort -f | uniq -ic | sort -n
